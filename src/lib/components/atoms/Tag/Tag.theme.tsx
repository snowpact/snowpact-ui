export type TagTheme = {
  items: {
    base: string;
  };
};

export const TAG_THEME: TagTheme = {
  items: {
    base: 'transition duration-500 ease-in-out hover:text-blue-400 text-gray-500'
  }
};
